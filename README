# Selfielapse
Selfielaps is a simple desktop app that runs in background.
It takes (at least it should :D) a photo per day (probably when you turn on your computer the first time in the day).


it use nw.js (ex node-webkit).

Work in progress.
